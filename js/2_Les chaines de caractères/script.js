/*
let string = "Ceci est une chaîne de caractère classique.";
// Pour écrire une chaîne de caractères, on l'entoure de ""

let anotherString = "Ceci en est une autre."; 

// On peut "coller" des chaines de caractères avec l'opérateur "+"
// On appelle cela la "concaténation"
let completeString = string + " " + anotherString;

// Si on souhaite afficher la phrase sur la page HTML, 
// on peut utiliser la méthode write de l'objet document
document.write(completeString);

// On peut connaitre la longueur d'une chaine de caractère
// grâce à la propriété length (valable sur presque tous 
// les objets javascript) 
let size = completeString.length;
document.write("la chaine fait " + size + " caractères");
// Noter l'abscence de parenthèse car il s'agit d'une propriété

// Si je souhaite chercher la place d'un caractère dans 
// une chaîne, je peux faire appel à la méthode "indexOf"
let dot = completeString.indexOf(".");
document.write("le premier point se trouve au caractère numéro" + dot);
// Généralement, on utilise indexOf() pour tester la présence
// d'un caractère dans une chaine. Si il ne se trouve pas dans 
// la chaine testée, indexOf() renverra une valeur négative 

// Si on souhaite découper notre chaine de caractère, on 
// utilisera la méthode split() 
let words = completeString.split(" ");
//console.log(words.length);
document.write("la chaine de caractère contient " + words.length + " mots");
*/


//Exercice 
//récupérer le contenu du formulaire

// Ecrire un script qui va vérifier que quelque chose a bien été saisi
   
// On ajoute pour une adresse mail et un bouton pour le soumettre
// de la même manière que précédemment, il va nous falloir récupérer son contenu. 
// Il faudra ensuite vérifier qu'il y a bien un @ et un . après l'@



